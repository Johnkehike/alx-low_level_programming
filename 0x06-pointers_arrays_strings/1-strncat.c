#include "main.h"
#include <string.h>
/**
 * *_strncat - entry
 * @dest: this
 * @src: that
 * @n: those
 * description: them
 *
 * comments
 * Return: watch
 *
 */
char *_strncat(char *dest, char *src, int n)
{
	return(strncat (dest, src, n));
	return(strncat (dest, src, n));
}
