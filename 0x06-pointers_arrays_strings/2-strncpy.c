#include "main.h"
#include <string.h>
/**
 * _strncpy - entry
 * @dest: 
 * @src:
 * @n:
 * description:
 *
 * Strncpy command
 * Return:
 *
 *
 */
char *_strncpy(char *dest, char *src, int n)
{
	return(strncpy(dest, src, n));
	return(strncpy(dest, src, n));
}
