#include "main.h"
#include <string.h>
/**
 * *_strcat - entry
 * @dest:this
 * @src:that
 * description: those
 *
 * Concatenate Strings
 * Return:0
 *
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest,src));
}
